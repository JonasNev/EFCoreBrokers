@model CompanyModel
<link href="D:\Programavimas\NTBrokers\NTBrokers\Content\Chosen\chosen.min.css" rel="stylesheet" />
<h2>Add a company</h2>

@using (Html.BeginForm("Create", "Company", FormMethod.Post))
{
    <div class="form-group">
        @Html.LabelFor(c => c.Name, "Name")
        @Html.TextBoxFor(c => c.Name, new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.LabelFor(c => c.City, "City")
        @Html.TextBoxFor(c => c.City, new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.LabelFor(c => c.Street, "Street")
        @Html.TextBoxFor(c => c.Street, new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.LabelFor(c => c.Number, "No.")
        @Html.TextBoxFor(c => c.Number, new { @class = "form-control" })
    </div>
    <div class="form-group">
        <label asp-for="Brokers[0].Id" class="control-label">Brokers</label>
        <div>
            @*MultiSelect Dropdown*@
            @Html.ListBoxFor(model => model.BrokerIds, new MultiSelectList(@Model.Brokers, "Id", "Name"), new { id = "multiSelectDropDown", multiple = "multiple", style = "width:250px" })
        </div>
    </div>
    @Html.HiddenFor(c => c.Companies[0].Id)
    <button type="submit" class="btn btn-primary">Add</button>
    @Html.ActionLink("Cancel", "Index", "Company", null, new { @class = "btn btn-secondary" })
}

@section scripts {
    <script>
        $("#multiSelectDropDown").chosen();
    </script>
}
